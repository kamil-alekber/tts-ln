# TTS Base Image Version
# Set this to a specific version tag (e.g., v1.0.0) or leave as 'latest'
TTS_BASE_VERSION=latest

# SSH Configuration for sync worker
SSH_USER=root
SSH_PASSWORD=your_ssh_password_here
SSH_HOST=192.168.8.184
RSYNC_DESTINATION_PATH=/srv/samba/k8s-share/media/books/

# Docker Hub Credentials (for GitHub Actions)
# Add these as secrets in your GitHub repository:
# - DOCKER_USERNAME
# - DOCKER_PASSWORD
